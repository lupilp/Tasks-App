{
  "version": 2,
  "builds": [
    {
      "src": "babel src -d dist && ncp src/views dist/views && ncp src/public dist/public",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/main.hbs"
    }
  ]
}
